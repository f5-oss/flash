// assets/icon-animations.js
(function(){
  // This file mirrors the injected inline script; kept for separation if you prefer external file usage.
  // It is intentionally lightweight; index.html already injects core behavior.
  console.log('Icon animations asset loaded');
})();
